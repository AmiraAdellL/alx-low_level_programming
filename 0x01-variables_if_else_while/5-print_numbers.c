#include <stdio.h>
/**
 * main - start
 * Description: Write a program that prints all single digit numbers
 * Return: (0) Success
 */

int main(void)
{
	int N;

	for (N = 0; N < 10; N++)
	{
		printf(N);
	}
	printf('\n');
	return (0);
}
