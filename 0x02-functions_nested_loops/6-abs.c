#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 * @n: the number to get its absolute
 * Return: Absolute value
*/
int _abs(int n)
{
	int abs;

	if (n < 0)
	{
		abs = n * -1;
		return (abs);
	}
	return (0);

}
